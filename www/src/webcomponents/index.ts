// Side-effect import: registers <pdf-page-card>, <pdf-document-view> and
// <pdf-editor-app> via customElements.define(). Import this once (e.g. from
// main.ts) before any of these tags are used in the page.
import "./pdf-page-card";
import "./pdf-document-view";
import "./pdf-editor-app";
